<%= form_for @currency_conversion, html: {class: "form-horizontal"} do |f| %>
  <% if @currency_conversion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@currency_conversion.errors.count, "error") %> prohibited this currency_conversion from being saved:</h2>

      <ul>
      <% @currency_conversion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :date, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :date, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :amount, class: 'col-sm-2 control-label' %>
      <%= f.number_field :amount, class: 'form-control' %>
    </div>

  <div class="form-group">
    <%= f.label :from , class: 'col-sm-2 control-label' %>
    <%= collection_select(:currency_conversion, :from_code, @currencies, :code, :code) %>
  </div>

  <div class="form-group">
    <%= f.label :to , class: 'col-sm-2 control-label' %>
    <%= collection_select(:currency_conversion, :to_code, @currencies, :code, :code) %>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit('Convert', class: 'btn btn-primary form-control') %>
    </div>
  </div>
<% end %>
